import styled from 'styled-components';

export const RoundedButton = styled.button`
  border: none;
  border-radius: 10px;
  padding: 10px 20px;
  background-color: #fff;
  color: var(--purple);
  font-size: 16px;
  cursor: pointer;
`;
